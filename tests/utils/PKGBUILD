pkgname="${PKGNAME:-}"
pkgver=$package_ver
pkgrel=$package_rel
pkgdesc=${PKGDESC:-"This is test package description"}
arch=('x86_64')
license=('GPL')
md5sums=('SKIP')
depends=()
makedepends=(
)
backup=()
branch=${BRANCH:-master}
source=(
    "PKGBUILD"
)

build() {
	true
}

package() {
	true
}
